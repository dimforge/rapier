    /// Vertex-vs-surface contact rows: the surface vertices of `other` (itself for self
    /// simulated) against the surface of awake body `ai`, one row per (vertex, element) the
    /// the other's surface, for an other body that is not in the awake list). A row holds the
        // already pass through each other, the pair's keep-apart rows are wrong-sided by
        // the self stand-down opened), so they stand down around it, like the self rows do
            // boundary, the particles of the cell carrying it when it collides through a skin.
                // A candidate the contact-modification hook disabled gets no constraint.
                // would pin the intruder inside; the volume rows and the elasticity
