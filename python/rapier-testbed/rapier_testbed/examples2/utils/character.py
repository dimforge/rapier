"""Minimal placeholder for the Rust ``utils::character`` helper.

The Rust example wires a manually-driven character through the testbed's
keyboard and ``egui`` UI layers. The Python mini-testbed has neither, so
this helper is intentionally empty — examples that originally called
``character::update_character`` just build a static scene in Python.
"""
from __future__ import annotations

__all__: list[str] = []
